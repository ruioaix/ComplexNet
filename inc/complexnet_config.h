//if define NDEBUG, all assert will not work.
//if not define NDEBUG, all assert will work.
//#define NDEBUG 

//if define VEXTER_FILE_DIRECTION_LEFT, data file's column 2 point to column 1, direction is left.
//if not define, column 1 points column 2. that's normal.
#define VEXTER_FILE_DIRECTION_LEFT

//if define TOUCHTYPE_PART, touchtype will be part type.
//if not define TOUCHTYPE_PART, touchtype will be all type.
#define TOUCHTYPE_PART
